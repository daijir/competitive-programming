"""
==============================================================================
paiza B136: チョコのお返し
==============================================================================

【問題の概要】
H × W の座席があり、初期位置 (sy, sx) から移動経路文字列 s (長さ N) に従って
1マスずつ移動する。各移動ごとに、訪れた座席にいるクラスメイトからもらった
チョコの個数を順番に出力する。

------------------------------------------------------------------------------
■ Divide and Conquer (分割統治法 / 問題解決思考) の考え方
------------------------------------------------------------------------------
「困難は分割せよ」(ルネ・デカルト)
分割統治法（Divide and Conquer）は、プログラミングやアルゴリズム設計における
最も基本的かつ強力なパラダイムです。

1. Divide (分割):
   - 大きな問題を、より小さく解きやすい独立した「サブ問題」に分解する。
   - 【本問での適用】:
     ① プログラム全体の責務を「入力解析」「移動シミュレーション」「結果出力」に分割する。
     ② 「N回の連続移動」という全体処理を、「1回の移動とチョコ取得」という最小単位の
        サブステップに分解する。

2. Conquer (統治・解決):
   - 分割したサブ問題をそれぞれ個別に解く。
   - 【本問での適用】:
     「1回の移動」は、現在地 (cur_y, cur_x) に方向ベクトル (dy, dx) を加算し、
     その座標のチョコ数を取得するだけ（O(1)の単純な問題に帰着される）。

3. Combine (統合):
   - 各サブ問題の解答をまとめ、最終的な全体の答えを構築する。
   - 【本問での適用】:
     各ステップで得られたチョコの個数を結果リストに蓄積し、指定のフォーマットで出力する。

※ アルゴリズム理論における狭義の分割統治法（マージソートや二分探索など）は
   「再帰的に半分に割って統治する」ものですが、広義の分割統治の核は
   『複雑な問題を扱いやすい単位に切り分けて個別に制圧する』ことにあります。

------------------------------------------------------------------------------
■ 本コードで活用している Python の基礎知識・テクニック
------------------------------------------------------------------------------
1. 高速な一括標準入力 (`sys.stdin.read().split()`):
   - `input()` をループ内で何度も呼ぶとI/Oオーバーヘッドが大きくなります。
   - `sys.stdin.read().split()` は、入力全体を1度で読み込み、空白や改行でトークンに
     自動分割するため、実行時間が大幅に短縮されます (競技プログラミングの定石)。

2. 0-indexed (0始まり) と 1-indexed (1始まり) の補正:
   - 問題文では座標が 1始まり（1 ≦ sy ≦ H, 1 ≦ sx ≦ W）で与えられます。
   - Pythonのリストは 0始まり（0 ～ len-1）のため、`sy - 1`, `sx - 1` で補正します。

3. 辞書 (dict) による方向ベクトルのマッピング:
   - `if move == 'F': ... elif move == 'B': ...` と書く代わりに、
     `DIRECTIONS = {'F': (-1, 0), ...}` と辞書で定義すると、分岐処理がなくなり
     可読性と保守性が向上します。

4. タプルのアンパック代入:
   - `dy, dx = DIRECTIONS[move]` のように、タプルの要素を一度に別々の変数へ展開できます。

5. 2次元リスト内包表記:
   - `[tokens[offset : offset + W] for i in range(H)]` のように、
     行ごとにスライスして2次元グリッドを簡潔に生成できます。
==============================================================================
"""

import sys


def main():
    input_data = sys.stdin.read().split()

    if not input_data:
        return
    
    # あなたが移動する回数
    n = int(input_data[0])
    # クラスの席の縦の数
    h = int(input_data[1])
    # クラスの席の横の数
    w = int(input_data[2])

    cur_y = int(input_data[3]) - 1
    cur_x = int(input_data[4]) - 1

    moves = input_data[5]

    grid_tokens = input_data[6 : 6 + h * w]
    grid = [
        [int(grid_tokens[r * w + c]) for c in range(w)]
        for r in range(h)
    ]

    directions = {
        'F': (-1, 0),
        'B': (1, 0),
        'L': (0, -1),
        'R': (0, 1)
    }

    results = []

    for move in moves:
      dy, dx = directions[move]
      cur_y += dy
      cur_x += dx
      
      chocolates = grid[cur_y][cur_x]
      results.append(chocolates)

    print('\n'.join(map(str, results)))


if __name__ == '__main__':
    main()